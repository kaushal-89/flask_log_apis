# flask_log_apis
Flask REST API for Log File Data Access and Analysis. This implementation reads and parses all logs on startup into memory. For very large datasets consider    streaming, a database, or an indexed format. Pagination is supported via page &amp; per_page.
